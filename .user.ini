
    upload_max_filesize = 1024M
    post_max_size = 1024M
    memory_limit = 512M
    upload_max_size = 1024M

    set_time_limit = 0 ; // กำหนดเวลาการประมวลผล script (0 หมายถึงไม่กำหนดเวลาการทำงาน)
    max_input_time = 1800; // กำหนดเวลาการทำงานสูงสุดกับการส่งค่าด้วย $_GET $_POST และ $_FILES (วินาที)
    max_execution_time = 9000; // กำหนดเวลาการประมวลผล script (วินาที)
    display_errors = On

    wincache.maxfilesize = 2048
   
